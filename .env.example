# Copy the relevant example under config/environments/ to .env for local use.
# Secrets are supplied through environment variables and must not be committed.
PATHLIGHT_PROJECT_ROOT=
JIRA_CLIENT_ID=
JIRA_REDIRECT_URI=http://127.0.0.1:4242/api/jira/callback
NODE_ENV=development
